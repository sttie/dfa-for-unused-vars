package lexing

enum class TokenType {
    ID,
    IF,
    WHILE,
    END,
    INTEGER,
    PLUS,
    MINUS,
    MULT,
    DIV,
    GREATER,
    LESS,
    ASSIGN,
    LBRACKET,
    RBRACKET,
    ENDFILE,
    NEWLINE,
    EMPTY,
}